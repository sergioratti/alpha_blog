<h1 align="center">Welcome to <%= @user.username %></h1>
<div class="row">
    <div class="col-md-4 col-md-offset-4 center">
        <%= gravatar_for @user, size: 160 %>
    </div>
    
</div>
<h4 align="center"><%= @user.username %>'s article(s)</h4>
<%= render 'articles/articles', obj: @user.articles %>